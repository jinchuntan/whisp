# Dev/test tooling for the web/API package. Install with the runtime deps:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

# Local ASGI dev server (used by `make dev`; NOT deployed to Vercel).
uvicorn[standard]>=0.30,<1.0

pytest>=8.2,<9.0
pytest-asyncio>=0.23,<1.0
ruff>=0.5,<1.0
mypy>=1.10,<2.0
