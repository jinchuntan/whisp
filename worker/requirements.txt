# ============================================================================
# Persephone WORKER dependencies (WSL2 / Linux ONLY — never installed on Vercel).
# ============================================================================
supabase>=2.6,<3.0
httpx>=0.27,<0.29
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0

# Local transcription. faster-whisper bundles CTranslate2 + an audio decoder,
# so no separate ffmpeg install is required for standard PCM WAV input.
faster-whisper>=1.0,<2.0

# Semantic clustering (local embeddings; pulls torch + numpy transitively).
sentence-transformers>=2.7,<4.0

# --- Agora (OPTIONAL, Linux/glibc only). NOT installed here by default.
#     To enable the real media bridge, install the separate optional file:
#         pip install -r requirements-agora.txt
#     Requires real Agora credentials + credit and AGORA_LIVE_ENABLED=true.
#     Windows is NOT supported — use WSL2 Ubuntu. See docs/AGORA_SETUP.md.
