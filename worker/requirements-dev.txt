# Worker test/tooling deps. Deliberately EXCLUDES faster-whisper /
# sentence-transformers / the Agora SDK so the worker test suite runs fast and
# proves those heavy imports are lazy (never imported at module load).
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
httpx>=0.27,<0.29
pytest>=8.2,<9.0
pytest-asyncio>=0.23,<1.0
ruff>=0.5,<1.0
mypy>=1.10,<2.0
