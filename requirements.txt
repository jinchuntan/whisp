# ============================================================================
# Persephone WEB/API dependencies (Vercel deployment + API test suite).
#
# LIGHTWEIGHT ONLY. Do NOT add faster-whisper, torch, sentence-transformers,
# numpy, or any Agora media SDK here — those belong in worker/requirements.txt
# and must never be installed into the Vercel bundle.
# ============================================================================
fastapi>=0.115,<1.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
supabase>=2.6,<3.0
httpx>=0.27,<0.29

# NOTE: no ASGI server here — Vercel provides its own runtime and imports `app`.
# uvicorn (for local `make dev`) lives in requirements-dev.txt so the Vercel
# function bundle stays minimal.
